<script context="module">
	import { browser, dev } from '$app/env';

	// we don't need any JS on this page, though we'll load
	// it in dev so that we get hot module replacement...
	export const hydrate = dev;

	// ...but if the client-side router is already loaded
	// (i.e. we came here from elsewhere in the app), use it
	export const router = browser;

	// since there's no dynamic data here, we can prerender
	// it so that it gets served as a static asset in prod
	export const prerender = true;
</script>

<svelte:head>
	<title>About</title>
</svelte:head>

<p>Busy? Thirsty? Mates impossible to organise? Then this is the tool for you. Find a day that works for everyone in minutes.</p>

<p>This app was created out of necessity when the author realised his mates were nigh on impossible to organise.</p>